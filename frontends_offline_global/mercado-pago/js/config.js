/**
 * ⚙️ Configuración del Frontend de Mercado Pago
 * Cambia IS_MOCK_MODE a 'false' para conectar con un backend real.
 */
const CONFIG = {
  API_BASE: '',
  IS_MOCK_MODE: true,
};

window.CONFIG = CONFIG;
