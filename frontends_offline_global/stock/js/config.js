/**
 * ⚙️ Configuración del Frontend de Stock Pro
 * Cambia IS_MOCK_MODE a 'false' para conectar con un backend real.
 */
const CONFIG = {
  API_BASE: '',
  IS_MOCK_MODE: true,
  THEME_DEFAULT: 'dark',
  LANG_DEFAULT: 'es',
};

window.CONFIG = CONFIG;
